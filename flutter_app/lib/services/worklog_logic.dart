/// Kept as a compatibility-style service layer so the Office app follows the
/// same service-oriented layout as the reference project.
class OfficeLogic {
  static String money(num value) => value.toStringAsFixed(2);
}
