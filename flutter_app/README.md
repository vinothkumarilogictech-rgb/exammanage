# Office Management Flutter App

This Flutter app follows the same layered structure used by the WorkLog mobile project:
- `config/` for API configuration
- `services/` for HTTP/session/data services
- `providers/` for state
- `screens/` for UI
- `widgets/` for reusable UI
- `models.dart` for API models

The app talks to the Flask backend at `/api/v1`.
